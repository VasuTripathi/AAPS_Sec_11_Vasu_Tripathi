Priority Queue = A special queue where each element has a priority.

Highest priority is served before others.

Java Implementation: PriorityQueue class.

Applications:

Dijkstra’s shortest path

Huffman coding

Task scheduling

Top K elements problems
Example Code:

java
Copy code
import java.util.PriorityQueue;

public class PriorityQueueExample {
    public static void main(String[] args) {
        PriorityQueue<Integer> pq = new PriorityQueue<>();
        pq.add(10);
        pq.add(2);
        pq.add(8);
        
        while (!pq.isEmpty()) {
            System.out.println(pq.poll());
        }
    }
}
Output:
2, 8, 10 (because min-heap by default)
Time Complexity:

Insert: O(log n)

Remove: O(log n)

Peek: O(1)
